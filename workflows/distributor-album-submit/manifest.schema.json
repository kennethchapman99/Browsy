{
  "$id": "distributor-album-submit/manifest.schema.json",
  "type": "object",
  "required": ["releaseTitle", "artistName", "releaseDate", "tracks"],
  "properties": {
    "releaseTitle": { "type": "string" },
    "artistName": { "type": "string" },
    "releaseDate": { "type": "string", "format": "date" },
    "label": { "type": "string" },
    "artworkPath": { "type": "string" },
    "trackCount": { "type": "integer", "minimum": 1 },
    "tracks": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["trackNumber", "trackTitle"],
        "properties": {
          "trackNumber": { "type": ["integer", "string"] },
          "trackTitle": { "type": "string" },
          "audioPath": { "type": "string" },
          "songwriter": { "type": "string" },
          "producer": { "type": "string" },
          "explicit": { "type": "boolean" },
          "instrumental": { "type": "boolean" },
          "isrc": { "type": ["string", "null"] }
        }
      }
    }
  }
}
